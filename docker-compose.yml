version: '2'

services:
  zeronet:
    build: ./
    read_only: false
    networks:
      - net
    ports:
      - 15441:15441
    volumes:
      - data:/data

volumes:
  data: {}

networks:
  net:
    external:
      name: firefox_net
